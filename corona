create table hospital (
	codhos integer primary key,
	nomhosp varchar(255) not null,
	telef varchar(12) not null,
	esthos varchar(2) not null,
	cidhos varchar(255) not null,
	ruahos varchar(255) not null
);

insert into hospital values (
1, 'Hospital Maria das Dores', '499840221300', 'SC', 'Romelandia', 'Avenida Brasil'
);


create table paciente (
	codpac integer primary key,
	nomepac varchar(255) not null,
	cpfpac varchar(11)not null unique,
	sexpac varchar(1) not null,
	telpac varchar(255) not null,
	datanasc date not null,
	peso integer not null,
	suspeito varchar(1) not null,
	positivado varchar(1) not null,
	
	codhos integer REFERENCES hospital(codhos)
);

select * from paciente;

insert into paciente values (
6, 'Zizi Boy', '02214511041', 'F', '47994711414', 85, 'S', 'S', 1, '2002-09-22'
);

create table cidade (
	codcid integer primary key,
	nomecid varchar(80),
	est varchar(80)
);

insert into cidade values (
	1, 'Maravilha', 'SC'
);

insert into cidade values (
	2, 'Pinhalzinho', 'SC'
);

insert into cidade values (
	3, 'Descanso', 'SC'
);

insert into cidade values (
	4, 'Maravilha', 'SC'
);

insert into cidade values (
	5, 'Chapeco', 'SC'
);

insert into endereco values(
	'Rua jardin', 'Jardim america', 'Casa do clevinho', 22, 4, 4
);


create table endereco (
	rua varchar(255) not null,
	bairro varchar(255) not null,
	comp varchar(255),
	num integer,
	codpac integer REFERENCES paciente(codpac),
	codcid integer REFERENCES cidade(codcid)
);

insert into sintomas values(
3, 'Dor de cabeça'
);

create table sintomas (
	codsint integer primary key,
	nomesint varchar (255) not null
);

insert into paciente_sintomas values(
	4, 3
);

create table paciente_sintomas (
	codpac integer REFERENCES paciente(codpac),
	codsint integer REFERENCES sintomas(codsint)
);

create table questionario (
	pesopac decimal(5,2) not null,
	altpac decimal(5,2) not null,
	bebe varchar(1) not null,
	fuma varchar(1) not null,
	relacsex varchar(1) not null,
	tipsang varchar(3) not null,
	
	codpac integer REFERENCES paciente(codpac)
);
